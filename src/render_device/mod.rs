//! Rust-side machinery for implementing `Noesis::RenderDevice`.
//!
//! Phase plan in `../../docs/PHASE_1_PLAN.md`. This module currently contains
//! only the Phase 1.1 type mirrors; the trait, vtable, and registration
//! factory land in subsequent sub-phases (1.3 + 1.5).

pub mod types;
